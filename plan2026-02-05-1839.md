# FAISS HNSW 优化计划 (2026-02-05)

## 已完成优化

| 优化项 | 提升 | 提交 |
|--------|------|------|
| batch_8 距离计算 + AVX-512 | +21% QPS | `363d3aad` |
| BFS 图重排序 (内存局部性) | +8.3% QPS | `8f0bb47a` |
| 软件预取 (PREFETCH_DISTANCE=8) | 包含在上述 | `a7b92801` |

**当前基线:** ~8,500 QPS (100K vectors, dim=128, M=32, efSearch=64)

---

## 待探索优化方向

### 1. 更激进的预取策略

**思路:** 当前只预取当前节点的邻居向量，可以更激进地预取"邻居的邻居"。

```cpp
// 当前: 预取 neighbors[i] 的向量
// 改进: 同时预取 neighbors[i] 的邻居列表
for (int i = 0; i < num_neighbors; i++) {
    // 预取邻居向量 (已有)
    _mm_prefetch(vector_data + neighbors[i] * dim, _MM_HINT_T0);
    // 新增: 预取邻居的邻居列表
    _mm_prefetch(neighbor_list_ptr(neighbors[i]), _MM_HINT_T1);
}
```

**预期收益:** 5-10% (减少邻居列表访问的 cache miss)

**风险:** 可能污染 cache，需要调优预取距离

---

### 2. SIMD 优先队列

**思路:** HNSW 搜索的热点是维护 top-k 候选集的堆操作。用 AVX-512 并行比较可加速。

```cpp
// 当前: std::priority_queue，逐个比较
// 改进: 用 SIMD 同时比较 16 个候选
__m512 candidates = _mm512_loadu_ps(candidate_distances);
__m512 threshold = _mm512_set1_ps(heap_top);
__mmask16 mask = _mm512_cmp_ps_mask(candidates, threshold, _CMP_LT_OS);
if (mask) {
    // 有更好的候选，更新堆
}
```

**预期收益:** 10-15% (heap 操作占搜索时间 ~20%)

**难度:** 高，需要重写堆数据结构

---

### 3. 图压缩

**思路:** 减少图结构的内存占用，提高 cache 效率。

选项:
- **邻居 ID 压缩:** 用 delta encoding + varint，节省 50%+ 空间
- **移除冗余边:** 剪枝低效边，减少遍历
- **分层存储:** 热点节点放 L1，冷节点放 L3

```cpp
// 当前: 每个邻居 ID 用 4 字节 (int32)
// 改进: delta encoding，平均 1-2 字节
// neighbors: [1000, 1005, 1003, 1050]
// delta:     [1000,    5,   -2,   47]  -> varint 编码
```

**预期收益:** 5-10% (减少内存带宽)

**风险:** 解码开销可能抵消收益

---

### 4. 搜索路径缓存

**思路:** 热点入口节点的搜索路径可以缓存复用。

```cpp
// 缓存从 entry_point 到各层的常用路径
struct PathCache {
    std::vector<storage_idx_t> cached_path[MAX_LEVEL];
    float cached_query[DIM];  // 上次查询向量
    float similarity_threshold = 0.9;  // 相似度阈值
};

// 如果新查询与缓存查询相似，复用路径
if (dot_product(query, cached_query) > similarity_threshold) {
    start_from_cached_path();
}
```

**预期收益:** 对相似查询可达 20-30%

**适用场景:** 查询有局部性时 (如推荐系统)

---

### 5. Level 0 专用优化

**思路:** HNSW 搜索 90%+ 时间在 level 0，针对性优化。

优化点:
- **Level 0 专用内存布局:** 连续存储，减少跳转
- **Level 0 批量距离计算:** 一次计算所有邻居距离
- **Level 0 SIMD 遍历:** 用 AVX-512 并行处理 16 个邻居

```cpp
// Level 0 专用搜索函数
void search_level0_optimized(const float* query, ...) {
    // 所有邻居向量连续存储
    // 用 batch_16 一次计算所有距离
    float distances[16];
    fvec_L2sqr_batch_16(query, neighbor_vectors, dim, distances);
    
    // SIMD 找最小值
    __m512 d = _mm512_loadu_ps(distances);
    int min_idx = _mm512_reduce_min_ps_idx(d);
}
```

**预期收益:** 15-25% (level 0 是热点)

**难度:** 中等，需要修改内存布局

---

### 6. 查询批处理优化

**思路:** 多个查询共享相同的图遍历路径前缀。

```cpp
// 当前: 每个查询独立搜索
// 改进: 批量查询共享上层遍历
void batch_search(const float* queries, int nq, ...) {
    // Step 1: 所有查询共享从 entry_point 到 level 1 的路径
    auto common_entry = find_common_entry(queries, nq);
    
    // Step 2: 在 level 0 并行搜索
    #pragma omp parallel for
    for (int q = 0; q < nq; q++) {
        search_level0(queries + q * dim, common_entry, ...);
    }
}
```

**预期收益:** 10-20% (减少重复遍历)

---

## 优先级排序

| 优先级 | 优化项 | 难度 | 预期收益 | 风险 |
|--------|--------|------|----------|------|
| 0 | VTune 性能分析 | 低 | 数据驱动 | 无 |
| 1 | Level 0 专用优化 | 中 | 15-25% | 低 |
| 2 | 更激进的预取 | 低 | 5-10% | 中 |
| 3 | SIMD 优先队列 | 高 | 10-15% | 中 |
| 4 | 查询批处理优化 | 中 | 10-20% | 低 |
| 5 | 图压缩 | 中 | 5-10% | 高 |
| 6 | 搜索路径缓存 | 低 | 场景依赖 | 低 |

---

### 7. VTune 性能分析驱动优化

**思路:** 用 Intel VTune Profiler 进行深度性能分析，找出真正的瓶颈。

**分析步骤:**

```bash
# 1. 编译带调试符号的 Release 版本
cmake .. -DCMAKE_BUILD_TYPE=RelWithDebInfo -DCMAKE_CXX_FLAGS="-g -march=native"

# 2. 运行 VTune Hotspots 分析
vtune -collect hotspots -result-dir vtune_hotspots \
    ./benchs/bench_hnsw_compare 100000 128 2000 64

# 3. 运行 VTune Memory Access 分析 (cache miss)
vtune -collect memory-access -result-dir vtune_memory \
    ./benchs/bench_hnsw_compare 100000 128 2000 64

# 4. 运行 VTune Microarchitecture 分析 (CPU 流水线)
vtune -collect uarch-exploration -result-dir vtune_uarch \
    ./benchs/bench_hnsw_compare 100000 128 2000 64
```

**关注指标:**

| 分析类型 | 关注指标 | 优化方向 |
|----------|----------|----------|
| Hotspots | Top 函数耗时占比 | 定位热点函数 |
| Memory Access | L1/L2/L3 cache miss rate | 预取策略、内存布局 |
| Memory Access | Memory bound % | 是否内存瓶颈 |
| Microarchitecture | Frontend bound % | 指令解码瓶颈 |
| Microarchitecture | Backend bound % | 执行单元瓶颈 |
| Microarchitecture | Bad speculation % | 分支预测失败 |

**预期发现:**

1. **距离计算函数** - 预计占 40-60%，已优化
2. **堆操作** - 预计占 15-25%，SIMD 优先队列可优化
3. **内存访问** - cache miss 热点，预取可优化
4. **分支预测** - visited 检查可能有分支失败

**输出:**

分析完成后更新本文档，添加:
- VTune 分析截图/数据
- 实际热点分布
- 基于数据的优化优先级调整

---

## 下一步行动

0. **运行 VTune 分析** - 数据驱动，找到真正瓶颈
1. **实现 Level 0 专用优化** - 收益最高，风险最低
2. **测试更激进的预取** - 实现简单，快速验证
3. **评估 SIMD 优先队列** - 先做 profiling 确认堆操作占比

---

## 性能目标

| 指标 | 当前 | 目标 |
|------|------|------|
| QPS (100K, dim=128) | 8,500 | 12,000+ |
| 相对基线提升 | +30% | +50%+ |

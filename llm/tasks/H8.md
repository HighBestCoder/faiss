# H8: HNSW16,{best} (M=16, 更少连接)

## 配置
- **ID**: H8
- **index_factory**: `HNSW16,{best}`
- **优化**: baseline
- **分支**: `bench/H8`
- **结果文件**: `llm/results/phase2/H8_HNSW16.md`
- **Phase**: 2
- **依赖**: Phase 1 H1-H7 结果 (选 Recall>=95% 且 QPS 最高的编码替换 `{best}`)

## 搜索参数
- efSearch=64,128,256

## 说明
- M=16, 更少连接, 更小图, 更低内存
- 需要先完成 Phase 1 (H1-H7) 确定最优编码

## 数据集
- cohere_medium_1m (1M, 768D, cosine)
- 查询: 1000 queries, ground truth k=1000

## 执行步骤
1. 查看 Phase 1 结果, 确定 `{best}` 编码
2. `git checkout v1.14.1 && git checkout -b bench/H8`
3. 编写/运行 benchmark
4. 记录结果到 `llm/results/phase2/H8_HNSW16.md`
5. git commit

## 状态
pending
